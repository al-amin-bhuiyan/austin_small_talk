// Add a minimal AppColors helper so shared widgets can reference common colors
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Common colors used across the app. Add or override as needed.
  static const Color whiteColor = Color(0xFFFFFFFF);
  static const Color blackColor = Color(0xFF000000);
  static const Color primaryColor = Color(0xFF0A84FF);
  static const Color transparent = Colors.transparent;
  static const Color profile_item_background = Color(0x4C464646);
  static const Color deepblue = Color(0xFF1E90FF); // Deep blue for Terms and Privacy Policy
static const Color logout =Color(0xFF3C3C3C);

}
