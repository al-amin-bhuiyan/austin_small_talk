import 'package:get/get.dart';

class SplashController extends GetxController {
  // This controller is mainly for dependency injection
  // Navigation logic is handled in the screen with static flag
  
  @override
  void onInit() {
    super.onInit();
    // Initialize any app-wide settings here if needed
  }
}
